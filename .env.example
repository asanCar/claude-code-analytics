POSTGRES_PASSWORD=changeme
CLAUDE_DIR=~/.claude
GRAFANA_PASSWORD=admin

# Optional: when running scripts/record-dashboards.ts, swap this username for
# 'demo' in sessions.project / project_path so recordings don't expose your
# real macOS path. Leave blank to skip path redaction.
REDACT_USERNAME=
